import * as React from 'react';
import { Link } from '@reach/router';

const PageNew = ({  }: any) => (
  <div>
    <span>New page</span>
    <Link to="/">Home</Link>
  </div>
);

export default PageNew;
